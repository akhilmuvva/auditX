name: AuditX CI Pipeline

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install System Dependencies (Slither & Mythril)
      run: |
        pip3 install --upgrade pip || true
        pip3 install "setuptools<60" || true
        pip3 install slither-analyzer mythril || echo "⚠️ Optional security binaries (Slither/Mythril) could not be installed. Skipping..."

    - name: Install Node Dependencies
      run: |
        npm ci
        npm install -g surya

    - name: Build Dashboard
      run: |
        cd dashboard
        npm ci
        npm run build

    - name: Run Basic Tests
      run: |
        # Run Jest unit tests
        npm test
        
        # If Hardhat test suite exists, run it
        if [ -f "hardhat.config.js" ] || [ -f "hardhat.config.ts" ] || [ -f "hardhat.config.cjs" ]; then
          npx hardhat test
        fi
